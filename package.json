{
  "name": "hello-world-api",
  "version": "1.0.0",
  "description": "A simple Node.js API with JWT authentication for AWS Lambda",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=development node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {},
  "author": "pankaja das",
  "license": "pankaja"
}